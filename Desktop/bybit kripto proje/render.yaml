# Render.com için otomatik deployment config
services:
  - type: web
    name: kripto-portfolio-risk
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.10.12

